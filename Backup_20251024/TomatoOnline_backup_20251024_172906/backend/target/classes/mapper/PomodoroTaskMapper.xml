<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.pomodorostudy.mapper.PomodoroTaskMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.pomodorostudy.entity.PomodoroTask">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="task_name" property="taskName" />
        <result column="description" property="description" />
        <result column="planned_duration" property="plannedDuration" />
        <result column="actual_duration" property="actualDuration" />
        <result column="status" property="status" />
        <result column="priority" property="priority" />
        <result column="category" property="category" />
        <result column="start_time" property="startTime" />
        <result column="end_time" property="endTime" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_id, task_name, description, planned_duration, actual_duration, status, priority, category, start_time, end_time, create_time, update_time
    </sql>

    <!-- 根据用户ID查询任务列表 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM pomodoro_task
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <!-- 根据用户ID和状态查询任务 -->
    <select id="selectByUserIdAndStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM pomodoro_task
        WHERE user_id = #{userId}
        AND status = #{status}
        ORDER BY create_time DESC
    </select>

    <!-- 查询进行中的任务 -->
    <select id="selectInProgressTasks" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM pomodoro_task
        WHERE status = 1
        ORDER BY start_time DESC
    </select>

    <!-- 根据分类查询任务 -->
    <select id="selectByCategory" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM pomodoro_task
        WHERE user_id = #{userId}
        AND category = #{category}
        ORDER BY create_time DESC
    </select>

    <!-- 查询高优先级任务 -->
    <select id="selectHighPriorityTasks" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM pomodoro_task
        WHERE user_id = #{userId}
        AND priority = 3
        AND status IN (0, 1)
        ORDER BY create_time ASC
    </select>

    <!-- 更新任务状态 -->
    <update id="updateTaskStatus">
        UPDATE pomodoro_task
        SET status = #{status},
            update_time = NOW()
        WHERE id = #{taskId}
    </update>

    <!-- 开始任务 -->
    <update id="startTask">
        UPDATE pomodoro_task
        SET status = 1,
            start_time = NOW(),
            update_time = NOW()
        WHERE id = #{taskId}
    </update>

    <!-- 完成任务 -->
    <update id="completeTask">
        UPDATE pomodoro_task
        SET status = 2,
            end_time = NOW(),
            actual_duration = #{actualDuration},
            update_time = NOW()
        WHERE id = #{taskId}
    </update>

    <!-- 取消任务 -->
    <update id="cancelTask">
        UPDATE pomodoro_task
        SET status = 3,
            end_time = NOW(),
            update_time = NOW()
        WHERE id = #{taskId}
    </update>

    <!-- 统计用户任务数量 -->
    <select id="countTasksByUser" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM pomodoro_task
        WHERE user_id = #{userId}
    </select>

    <!-- 统计已完成任务数量 -->
    <select id="countCompletedTasksByUser" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM pomodoro_task
        WHERE user_id = #{userId}
        AND status = 2
    </select>

    <!-- 查询今日任务 -->
    <select id="selectTodayTasks" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM pomodoro_task
        WHERE user_id = #{userId}
        AND DATE(create_time) = CURDATE()
        ORDER BY create_time DESC
    </select>

    <!-- 查询本周任务 -->
    <select id="selectWeekTasks" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM pomodoro_task
        WHERE user_id = #{userId}
        AND YEARWEEK(create_time, 1) = YEARWEEK(CURDATE(), 1)
        ORDER BY create_time DESC
    </select>

</mapper>
